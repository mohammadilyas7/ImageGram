import { FlowerBundleDirective } from './flower-bundle.directive';

describe('FlowerBundleDirective', () => {
  it('should create an instance', () => {
    const directive = new FlowerBundleDirective();
    expect(directive).toBeTruthy();
  });
});
